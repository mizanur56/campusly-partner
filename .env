# NOTE:
# - Vite loads env vars from `.env`, `.env.local`, `.env.[mode]`, `.env.[mode].local`
# - Keep `.env` for LOCAL overrides only.
# - Put shared defaults in `.env.development` / `.env.production` and copy keys into `.env.local`.
